name: krea
version: 1.1.0
description: "Krea image generation backend (Krea 2 Large + Medium + Medium Turbo foundation models). Requires KREA_API_KEY."
author: Cryozen
kind: backend
requires_env:
  - KREA_API_KEY
