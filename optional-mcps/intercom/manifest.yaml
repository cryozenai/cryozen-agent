# Cryozen-approved MCP catalog entry.
# Presence in this directory = approval. Merged via PR review.
manifest_version: 1

name: intercom
connector_slug: intercom_mcp
description: >-
  Conversations, tickets, and customer data from Intercom.
source: https://developers.intercom.com/docs/guides/mcp

# Official vendor-hosted remote MCP (URL-only — Cryozen never spawns a local
# process for this entry). Native OAuth 2.1 + Dynamic Client Registration;
# Cryozen's MCP client + mcp_oauth_manager handle discovery, PKCE, token
# exchange, and refresh.
transport:
  type: http
  url: https://mcp.intercom.com/mcp

auth:
  type: oauth

# Composer-suggestion triggers (desktop brand pills).
suggest:
  keywords:
    - intercom
  hosts:
    - intercom.com
    - intercom.io

post_install: |
  On first connection Cryozen opens a browser to authorize with
  Intercom (or run `cryozen mcp login intercom`). Approve access,
  then restart the session so tools load.
